# DevArena — frontend

See the [root README](../README.md) for setup, env vars, and scripts.

- `pnpm dev` — Vite dev server
- `pnpm typecheck` — `tsc -b`
- `pnpm lint` — eslint
- `pnpm build` — typecheck + production bundle

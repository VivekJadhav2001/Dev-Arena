# Copy to .env and fill in real values. Never commit .env.
VITE_BACKEND_URL=http://localhost:2001/api/v1
# Optional: socket base URL (defaults to VITE_BACKEND_URL without /api/v1).
VITE_SOCKET_URL=http://localhost:2001

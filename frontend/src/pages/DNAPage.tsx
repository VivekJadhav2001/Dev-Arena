import { useEffect, useState } from 'react'
import { LoaderCircle } from 'lucide-react'
import { dnaService } from '../services/dna.service'
import type { IDNA } from '../types'

export default function DNAPage(){const[dna,setDna]=useState<IDNA|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{dnaService.getMyDNA().then(setDna).catch((err:unknown)=>setError(err instanceof Error?err.message:'Could not load Developer DNA.'))},[]);if(error)return <div className="rounded-xl border border-danger/40 bg-danger/10 p-4 text-red-200">{error}</div>;if(!dna)return <div className="flex items-center gap-3 text-textMuted"><LoaderCircle className="animate-spin"/>Calculating your Developer DNA…</div>;return <div><p className="text-sm font-bold text-primary">DEVELOPER DNA</p><h1 className="mt-1 font-display text-4xl font-bold">{dna.persona}.</h1><p className="mt-3 max-w-2xl text-textMuted">{dna.personaReason} This is a transparent, deterministic reading of stored GitHub activity.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{dna.traits.slice(0,3).map(({label,score})=><div key={label} className="rounded-2xl border border-border bg-surface p-6"><p className="text-sm text-textMuted">{label}</p><p className="mt-3 text-4xl font-bold text-primary">{score}</p><p className="mt-3 text-sm text-textMuted">Derived from your connected GitHub signals</p></div>)}</div><div className="mt-6 rounded-2xl border border-border bg-surface p-6"><h2 className="font-bold">How we calculate it</h2><p className="mt-2 text-textMuted">Contribution rhythm, language breadth, repository activity and open-source signals are evaluated with fixed backend rules.</p></div></div>}

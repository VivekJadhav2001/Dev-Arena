import { Copy, Plus, Radio, Swords } from 'lucide-react'
import { Link } from 'react-router-dom'
export default function ArenaLobby(){return <div><p className="text-sm font-bold text-primary">LIVE CODE ARENA</p><h1 className="mt-1 font-display text-4xl font-bold">One prompt. One winner.</h1><p className="mt-2 max-w-2xl text-textMuted">Short, deterministic challenges make every result fair, fast, and easy to verify.</p><div className="mt-8 grid gap-6 md:grid-cols-2"><section className="rounded-2xl border border-primary/30 bg-primary/5 p-7"><Plus className="text-primary"/><h2 className="mt-5 text-2xl font-bold">Start a challenge</h2><p className="mt-2 text-textMuted">Create a private room and share the code with a developer.</p><Link to="/battle/DEV-2026" className="mt-7 inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-3 font-bold text-background"><Swords size={17}/>Create battle</Link></section><section className="rounded-2xl border border-border bg-surface p-7"><Radio className="text-secondary"/><h2 className="mt-5 text-2xl font-bold">Join with a room code</h2><p className="mt-2 text-textMuted">Your opponent sees the same puzzle and live progress.</p><div className="mt-7 flex gap-2"><input value="DEV-2026" readOnly className="min-w-0 flex-1 rounded-xl border border-border bg-background px-4 font-mono text-sm"/><Link to="/battle/DEV-2026" className="rounded-xl bg-secondary px-4 py-3 font-bold"><Copy size={17}/></Link></div></section></div><p className="mt-6 text-sm text-textSubtle">Server contract: create room → join room → socket progress updates → validated answer → persistent result.</p></div>}

import { BarChart3, Swords, Trophy, Dna, Sparkles, LogOut } from 'lucide-react'
import { NavLink, Outlet, useNavigate } from 'react-router-dom'
const navigation = [{ to: '/dashboard', label: 'Dashboard', icon: BarChart3 }, { to: '/dna', label: 'Developer DNA', icon: Dna }, { to: '/arena', label: 'Arena', icon: Swords }, { to: '/leaderboard', label: 'Leaderboard', icon: Trophy }, { to: '/wrapped', label: 'Wrapped', icon: Sparkles }]
export default function AppShell() { const navigate = useNavigate(); return <div className="min-h-screen bg-background text-text"><header className="sticky top-0 z-20 border-b border-border/80 bg-background/90 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6"><button onClick={() => navigate('/')} className="flex items-center gap-2 font-display text-xl font-bold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary text-background">&lt;/&gt;</span> DevArena</button><nav className="hidden items-center gap-1 md:flex">{navigation.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} className={({ isActive }) => `flex items-center gap-2 rounded-lg px-3 py-2 text-sm transition ${isActive ? 'bg-surfaceRaised text-primary' : 'text-textMuted hover:bg-surface hover:text-text'}`}><Icon size={16}/>{label}</NavLink>)}</nav><div className="flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-sm font-semibold">Alex Morgan</p><p className="text-xs text-primary">Level 12 · Builder</p></div><div className="grid h-9 w-9 place-items-center rounded-full bg-secondary font-bold">AM</div><button aria-label="Return to landing page" onClick={() => navigate('/')} className="text-textMuted hover:text-text"><LogOut size={18}/></button></div></div><nav className="flex gap-1 overflow-x-auto border-t border-border px-3 py-2 md:hidden">{navigation.map(({ to, label }) => <NavLink key={to} to={to} className={({ isActive }) => `whitespace-nowrap rounded-md px-3 py-1.5 text-sm ${isActive ? 'bg-surfaceRaised text-primary' : 'text-textMuted'}`}>{label}</NavLink>)}</nav></header><main className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><Outlet /></main></div> }
